{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {select = id; }; 
            CLASS = ForgeryPalette; 
            LANGUAGE = ObjC; 
            OUTLETS = {delegate = id; matrix = NSMatrix; }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = ForgeryPaletteWindow; LANGUAGE = ObjC; SUPERCLASS = NSPanel; }
    ); 
    IBVersion = 1; 
}